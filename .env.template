# Traefik configuration for external access to Pythia EDC demo
# Copy this file to .env and adjust as needed

# Domain for accessing services via Traefik
DOMAIN=localhost

# TLS/SSL configuration
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERT_RESOLVER=le
