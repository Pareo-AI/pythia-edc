.venv/
dist/
build/
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/
.DS_Store
.idea
.claude

# Local demo config profiles (may hold provider URLs / cert paths); examples are tracked.
demo.env
demo.*.env

# Local-only material salvaged from the old copy; never published.
/local-salvage/

# The Eclipse EDC Samples checkout that `./demo up` builds its connector jars from. Half a
# gigabyte of upstream source and fat jars; `up.sh` clones it here and EDC_SAMPLES_DIR overrides it.
/.edc-samples/
